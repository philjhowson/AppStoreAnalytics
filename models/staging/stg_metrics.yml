version: 2

models:
  - name: stg_metrics

    columns:
      - name: app
        tests: [not_null]

      - name: date
        tests: [not_null]

      - name: metric
        tests: [not_null]

    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - app
              - date
              - country
              - device
              - metric